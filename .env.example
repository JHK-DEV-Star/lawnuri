# LawNuri Environment Configuration
# .env is only for server-level defaults.
# All settings (API keys, debate config) are managed via Settings UI
# and stored in backend/data/settings.json.
# .env values are used as fallback if settings.json doesn't exist.

UVICORN_PORT=8000
